/**
 * @fileoverview This module is intentionally left blank.
 * The Firebase Admin SDK initialization has been moved directly into the
 * service files that require it (`gallery.service.ts` and `auth.service.ts`)
 * to ensure a robust, self-contained setup and prevent any potential for
 * module resolution or race condition issues in the Next.js environment.
 */
